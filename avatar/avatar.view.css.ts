namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $eve_avatar, {
		display: 'inline-flex',
		alignItems: 'center',
		justifyContent: 'center',
		width: rem( 2.5 ),
		height: rem( 2.5 ),
		borderRadius: '50%',
		overflow: 'hidden',
		flex: {
			shrink: 0,
		},
		boxSizing: 'border-box',
	} )

}
